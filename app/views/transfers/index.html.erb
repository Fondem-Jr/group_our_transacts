<div class="bg-light">
  <div class="card mb-3 p-3 flex-row justify-content-between border-top-0 t-total">
    <h5 class="text-muted m-0">Total transfer</h5>
    <p class="fw-bold m-0"><%= number_to_currency(@transfers.sum(:amount)) %></p>
  </div>

  <%= render @transfers %>

  <div class="downfoot">
      <%= link_to 'New Transfer' , new_transfer_path, class: "link-btn btn border border-white text-white d-block p-3 m-3 text-uppercase"%>
  </div>
  <br>
</div>