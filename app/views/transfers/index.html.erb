<h1>Transfers</h1>
<div>
  <div class="card mb-3 p-3 flex-row justify-content-between border-top-0" id="trn-total">
    <h5 class="text-muted m-0">Total Amount</h5>
    <p class="font-weight-bold m-0">$<%= number_with_precision(@transfers.sum(:amount), :precision => 2) %></p>
  </div>

<%= render @transfers %>

<%= link_to 'New Transfer' , new_transfer_path, class: "link-btn btn
                  btn-success"%>
<br>